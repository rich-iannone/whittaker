{
  "hash": "988fbe3fccbef90ad06c95d8af65a6bd",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: Stream a Polars LazyFrame\ndescription: \"Fit a GAM on a Polars LazyFrame, deferring data loading until fit time.\"\ntags:\n  - Scalability\n  - Data input\n  - Polars\nbread-crumbs: false\n---\n\n# Stream a Polars LazyFrame\n\n`PolarsGAM` integrates with Polars' lazy execution model, and you build up a query plan as a\n`LazyFrame`, then pass it directly to `fit()`. Data is materialized and streamed in chunks only when\nfitting begins, so complex transformations upstream are folded into a single efficient scan.\n\n## Prepare a LazyFrame\n\nBuild a large synthetic dataset as a Polars `DataFrame`, then convert it to a `LazyFrame`. In a real\nworkflow this might point at a Parquet file or a query chain that has not yet executed.\n\n::: {#692591a4 .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport polars as pl\nimport whittaker as wk\n\nrng = np.random.default_rng(23)\nn = 200_000\ndf = pl.DataFrame({\n    \"x1\": rng.uniform(0, 2 * np.pi, n),\n    \"x2\": rng.uniform(0, 1, n),\n    \"y\": np.sin(rng.uniform(0, 2 * np.pi, n)) + 0.5 * rng.uniform(0, 1, n) + rng.normal(0, 0.3, n),\n})\nlazy = df.lazy()\n```\n:::\n\n\n## Fit\n\nPass the `LazyFrame` directly to `fit()`. `PolarsGAM` triggers execution internally and streams it\nthrough the discretized-basis fitting algorithm.\n\n::: {#8b6360f0 .cell execution_count=2}\n``` {.python .cell-code}\nmodel = wk.PolarsGAM(\"y ~ s(x1, k=10) + s(x2, k=8)\").fit(lazy)\nmodel.summary()\n```\n:::\n\n\n## Inspect Row Count\n\nAfter fitting, `n_rows` reports how many observations were processed.\n\n::: {#d45ad1bb .cell execution_count=3}\n``` {.python .cell-code}\nmodel.n_rows\n```\n:::\n\n\n## Predict\n\nPredictions use a plain dict of arrays, with no Polars dependency at inference time.\n\n::: {#350f0677 .cell execution_count=4}\n``` {.python .cell-code}\nnew_data = {\"x1\": np.array([0.5, 1.5, 2.5]), \"x2\": np.array([0.2, 0.5, 0.8])}\npreds = model.predict(new_data)\npreds.values\n```\n:::\n\n\n## Interpret\n\n`PolarsGAM` uses the same discretized-basis approach as `BigGAM`, so memory usage is bounded\nregardless of `n`. Passing a `LazyFrame` defers Polars query execution until `fit()` triggers it,\nmeaning any upstream filter, join, or derived-column step runs only once as part of the streaming\nscan. `PolarsGAM` also accepts file paths directly (`.parquet`, `.csv`, `.ipc`, `.arrow`, `.ndjson`,\nand `.jsonl` extensions are all supported).\n\n",
    "supporting": [
      "stream-a-polars-lazyframe_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}