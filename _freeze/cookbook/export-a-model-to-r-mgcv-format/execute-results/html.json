{
  "hash": "44b17f618ccfe199b6afcd72bacc60f1",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Export a Model to R's mgcv Format\"\ndescription: \"Convert a fitted whittaker GAM to a dict that mirrors R's mgcv list structure for cross-language interoperability.\"\ntags:\n  - Deployment\n  - Serialization\nbread-crumbs: false\n---\n\n# Export a Model to R's mgcv Format\n\nTeams that use both Python and R often need to share fitted models across the language boundary.\n`wk.to_mgcv_dict()` converts a fitted whittaker `GAM` into a JSON-serializable dict whose keys match\nthose of the list returned by R's `mgcv::gam()`, making it straightforward to reconstruct or inspect\nthe model on the R side.\n\n## Fit a Model\n\nLoad the `wages` dataset and fit a Gamma GAM with two smooth terms.\n\n::: {#731aa5fd .cell execution_count=1}\n``` {.python .cell-code}\nimport json\nimport numpy as np\nimport whittaker as wk\n\n# Load dataset and fit Gamma GAM\ndata = wk.load_dataset(\"wages\")\nmodel = wk.GAM(\"wage ~ s(age) + s(experience)\", family=wk.Gamma()).fit(data)\n```\n:::\n\n\n## Export to mgcv Dict\n\n`to_mgcv_dict()` accepts any fitted `GAM` and returns a plain Python dict.\n\n::: {#e2b3f3ca .cell execution_count=2}\n``` {.python .cell-code}\nd = wk.to_mgcv_dict(model)\nlist(d.keys())\n```\n\n::: {.cell-output .cell-output-display execution_count=2}\n```\n['coefficients',\n 'sp',\n 'scale',\n 'scale.estimated',\n 'gcv.ubre',\n 'edf',\n 'edf.total',\n 'deviance',\n 'null.deviance',\n 'aic',\n 'n',\n 'p',\n 'converged',\n 'iter',\n 'method',\n 'formula',\n 'family',\n 'smooth',\n 'nsdf',\n 'intercept']\n```\n:::\n:::\n\n\n## Inspect Metadata\n\nThe top-level keys mirror mgcv's return structure. Examine the formula string, family, observation\ncount, and AIC.\n\n::: {#521706bc .cell execution_count=3}\n``` {.python .cell-code}\nd[\"formula\"]\n```\n\n::: {.cell-output .cell-output-display execution_count=3}\n```\n'wage ~ s(age) + s(experience)'\n```\n:::\n:::\n\n\n::: {#6a701312 .cell execution_count=4}\n``` {.python .cell-code}\nd[\"family\"]\n```\n\n::: {.cell-output .cell-output-display execution_count=4}\n```\n{'family': 'Gamma'}\n```\n:::\n:::\n\n\n::: {#1ff73012 .cell execution_count=5}\n``` {.python .cell-code}\nd[\"n\"]\n```\n\n::: {.cell-output .cell-output-display execution_count=5}\n```\n800\n```\n:::\n:::\n\n\n::: {#39c601f5 .cell execution_count=6}\n``` {.python .cell-code}\nd[\"aic\"]\n```\n\n::: {.cell-output .cell-output-display execution_count=6}\n```\n5881.682229305514\n```\n:::\n:::\n\n\n## Inspect Smooth Structure\n\nThe `\"smooth\"` key holds a list of dicts, one per smooth term, matching mgcv's `$smooth` list.\n\n::: {#1cac8b8a .cell execution_count=7}\n``` {.python .cell-code}\n[s[\"term\"] for s in d[\"smooth\"]]\n```\n\n::: {.cell-output .cell-output-display execution_count=7}\n```\n[['age'], ['experience']]\n```\n:::\n:::\n\n\n## Serialize to JSON\n\nThe dict is fully JSON-serializable. NumPy arrays are converted with a default handler.\n\n::: {#3aba7326 .cell execution_count=8}\n``` {.python .cell-code}\njson_str = json.dumps(d, default=lambda x: x.tolist() if hasattr(x, \"tolist\") else x)\njson_str[:200]\n```\n\n::: {.cell-output .cell-output-display execution_count=8}\n```\n'{\"coefficients\": [3.710855878570552, 0.03308637526578651, 1.1212288473205227, 0.016796029126231817, -0.12874252230000624, -0.005325749674779646, 0.036716384619632704, 1.732608499446602e-06, 0.01606049'\n```\n:::\n:::\n\n\n## Round-Trip Back to whittaker\n\n`from_mgcv_dict()` reconstructs a prediction-ready `GAM` when the original data is supplied to refit\nthe smooth bases.\n\n::: {#462d2417 .cell execution_count=9}\n``` {.python .cell-code}\n# Reconstruct model from dict and predict\nmodel2 = wk.from_mgcv_dict(d, data=data)\nmodel2.predict({\"age\": np.array([30, 40, 50]), \"experience\": np.array([5, 10, 15])}).values\n```\n\n::: {.cell-output .cell-output-display execution_count=9}\n```\narray([28.11329697, 47.88889654, 67.12671993])\n```\n:::\n:::\n\n\n## Interpret\n\n`to_mgcv_dict()` produces a dict with the same keys as R's `mgcv::gam()` return value, making it\npossible to load a Python-fitted GAM into R for further analysis or reporting. The round-trip\nthrough `from_mgcv_dict()` is useful when you want to reload a model from a stored JSON snapshot\nwithout writing a binary file. Use `data=None` for a lightweight container that carries coefficients\nand metadata but cannot predict, or supply the original data to get a fully functional model back.\n\n",
    "supporting": [
      "export-a-model-to-r-mgcv-format_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}