{
  "hash": "73146839658d9427883b229e216d14df",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"[ConvexPSpline]{.doc-object-name .doc-class .doc-label .doc-label-class}\"\nbody-classes: doc-api-page\npage-navigation: false\nhtml-table-processing: none\n---\n\n::: {.doc-subject}\n\nShape-constrained P-spline: convex or concave.\n:::\n\n::: {.doc-usage-source}\nUsage\n\n[Source](https://github.com/rich-iannone/whittaker/blob/main/whittaker/smooths/monotone.py#L126-L239){target=\"_blank\" rel=\"noopener\"}\n:::\n\n::: {.doc-signature .doc-Kind.CLASS}\n```python\nConvexPSpline(k=20, degree=3, m=2, concave=False)\n```\n:::\n\n::: {.doc-text}\n\nUses the same B-spline basis and difference penalty as `~whittaker.smooths.pspline.PSpline`, but\nadditionally requires the fitted curve `f(x) = \\sum_j \\beta_j B_j(x)` to be convex (or, with\n`concave=True`, concave) over the whole domain. As with `MonotonePSpline`, this is enforced as\na linear inequality on the coefficients: for an equally-spaced B-spline basis, the curve is\nconvex whenever the second differences of the coefficients,\n`\\Delta^2 \\beta_j = \\beta_j - 2\\beta_{j-1} + \\beta_{j-2}`, are all non-negative. Use\n`ConvexPSpline` (via `s(x, bs=\"cx\")` for convex or `s(x, bs=\"cv\")` for concave in a formula)\nwhen the relationship is known to have a single bend of consistent curvature — e.g. a cost\ncurve, a learning curve, or a concave production function — and an unconstrained smooth would\notherwise produce spurious inflection points from noise.\n:::\n\n## Parameters {.doc-parameters}\n\n::: {.doc-definition-items}\n<code>[k]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [int]{.doc-parameter-annotation} [=]{.doc-parameter-default-sep .op} [[20]{.dv}]{.doc-parameter-default}</code>\n\n:   Number of B-spline basis functions. The default is `20`.\n\n<code>[degree]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [int]{.doc-parameter-annotation} [=]{.doc-parameter-default-sep .op} [[3]{.dv}]{.doc-parameter-default}</code>\n\n:   B-spline polynomial degree. The default is `3` (cubic).\n\n<code>[m]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [int]{.doc-parameter-annotation} [=]{.doc-parameter-default-sep .op} [[2]{.dv}]{.doc-parameter-default}</code>\n\n:   Difference penalty order. The default is `2`.\n\n<code>[concave]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [bool]{.doc-parameter-annotation} [=]{.doc-parameter-default-sep .op} [[False]{.va}]{.doc-parameter-default}</code>\n\n:   If `True`, enforce concavity. The default is `False` (convex).\n:::\n\n## Notes {.doc-notes}\n\nAs with `MonotonePSpline`, the constraint is enforced during P-IRLS fitting by projecting the\nordinary coefficient update onto the convex (or concave) cone after each iteration: smooth terms\nwhose basis is a `ConvexPSpline` have their coefficient block passed through `project_convex`\nbefore the next iteration's linear predictor is formed (see `whittaker.fitting.pirls`). The\nprojection extends the monotone PAVA projection by one order of differencing: convexity\nrequires the *first* differences of the coefficients, `d_j = \\beta_j - \\beta_{j-1}`, to form a\nnon-decreasing sequence (equivalently, that the second differences of `\\beta` are\nnon-negative), so `project_convex` computes the first differences, projects *them* onto the\nmonotone cone with PAVA (see the `Notes` on `MonotonePSpline` for the algorithm), and then\nreconstructs `\\beta` by cumulatively summing the projected differences back up from `\\beta_0`.\nConcavity is handled by negating the differences before and after the PAVA projection,\nmirroring `decreasing=True` for `MonotonePSpline`.\n\n## Examples {.doc-examples}\n\n::: {#9477ac61 .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport whittaker as wt\n\nrng = np.random.default_rng(0)\nx = np.sort(rng.uniform(-1, 1, 200))\ny = x**2 + rng.normal(scale=0.1, size=200)\n\nmodel = wt.GAM(\"y ~ s(x, bs='cx')\").fit({\"x\": x, \"y\": y})\n\nnew_x = np.linspace(-1, 1, 50)\nfitted = model.predict({\"x\": new_x}).values\nsecond_diff = np.diff(fitted, n=2)\n# Second differences are non-negative up to fitting/projection tolerance.\nnp.all(second_diff >= -1e-2)\n```\n\n::: {.cell-output .cell-output-display execution_count=1}\n```\nnp.True_\n```\n:::\n:::\n\n\n## Attributes {.doc-attributes}\n\n<table class=\"gd-summary-table\">\n<thead>\n  <tr>\n    <th>Name</th>\n    <th>Description</th>\n  </tr>\n</thead>\n<tbody>\n  <tr>\n    <td><a href=\"#constraint_direction\" class=\"doc-attribute doc-label doc-label-property\">constraint_direction</a></td>\n    <td>Sign convention used by the second-difference PAVA projection.</td>\n  </tr>\n  <tr>\n    <td><a href=\"#constraint_order\" class=\"doc-attribute doc-label doc-label-property\">constraint_order</a></td>\n    <td>Order of the difference constraint enforced by projection.</td>\n  </tr>\n</tbody>\n</table>\n\n### [constraint_direction]{.doc-object-name .doc-attribute .doc-label .doc-label-property}\n\n::: {.doc-subject}\n\nSign convention used by the second-difference PAVA projection.\n:::\n\n::: {.doc-signature .doc-attribute}\n<code>[constraint_direction]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [int]{.doc-parameter-annotation}</code>\n:::\n\n::: {.doc-text}\n\nReturns `-1` when `concave=True` (the first differences of the coefficients are negated\nbefore and after the PAVA projection, yielding non-positive second differences) or `1`\nfor the default convex case. Consulted by `project_convex` and by\n`whittaker.fitting.pirls` when projecting this term's coefficient block each iteration.\n:::\n\n### [constraint_order]{.doc-object-name .doc-attribute .doc-label .doc-label-property}\n\n::: {.doc-subject}\n\nOrder of the difference constraint enforced by projection.\n:::\n\n::: {.doc-signature .doc-attribute}\n<code>[constraint_order]{.doc-parameter-name}[:]{.doc-parameter-annotation-sep} [int]{.doc-parameter-annotation}</code>\n:::\n\n::: {.doc-text}\n\nAlways `2`: convexity/concavity is a constraint on the *second* differences of the\ncoefficients (equivalently, the first differences must form a monotone sequence), as\nopposed to `MonotonePSpline`'s first-order (`1`) constraint.\n:::\n\n## Methods {.doc-methods}\n\n<table class=\"gd-summary-table\">\n<thead>\n  <tr>\n    <th>Name</th>\n    <th>Description</th>\n  </tr>\n</thead>\n<tbody>\n  <tr>\n    <td><a href=\"#null_space_dimension\" class=\"doc-function doc-label doc-label-method\">null_space_dimension()</a></td>\n    <td>Return the dimension of the basis's unpenalized null space.</td>\n  </tr>\n</tbody>\n</table>\n\n### [null_space_dimension()]{.doc-object-name .doc-function .doc-label .doc-label-method}\n\n::: {.doc-subject}\n\nReturn the dimension of the basis's unpenalized null space.\n:::\n\n::: {.doc-usage-source}\nUsage\n\n[Source](https://github.com/rich-iannone/whittaker/blob/main/whittaker/smooths/monotone.py#L227-L239){target=\"_blank\" rel=\"noopener\"}\n:::\n\n::: {.doc-signature .doc-Kind.FUNCTION}\n```python\nnull_space_dimension()\n```\n:::\n\n::: {.doc-text}\n\nAs with `MonotonePSpline`, the shape constraint is enforced by post-hoc projection of the\ncoefficients rather than by removing degrees of freedom from the penalty, so this basis\nreports `0` unpenalized dimensions.\n:::\n\n#### Returns {.doc-returns}\n\n::: {.doc-definition-items}\n<code> [int]{.doc-parameter-annotation}</code>\n\n:   Always `0`.\n:::\n\n",
    "supporting": [
      "ConvexPSpline_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}